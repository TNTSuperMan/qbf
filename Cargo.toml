[package]
name = "qbf"
version = "0.1.0"
edition = "2021"

[dependencies]

[features]
default = ["debug"]
debug = []

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
